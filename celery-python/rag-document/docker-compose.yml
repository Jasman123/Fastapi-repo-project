services:

  redis:
    image: redis:7-apline
    container_name: rag-redis
    port: 
      -"6379:6379"
    volumens:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retris: 5

volumes:
  redis-data: